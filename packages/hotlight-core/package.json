{
  "name": "hotlight-core",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild --bundle src/index.ts --outfile=www/main.js --servedir=www --format=esm",
    "build": "esbuild src/index.ts --bundle --outfile=www/build.js --format=esm",
    "build:prod": "npm run build -- --minify",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --env jsdom",
    "test:watch": "npm test -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "jest": {
    "moduleNameMapper": {
      "\\.(css|scss)$": "identity-obj-proxy"
    }
  },
  "devDependencies": {
    "@babel/core": "^7.15.8",
    "@babel/plugin-transform-modules-commonjs": "^7.15.4",
    "@babel/preset-typescript": "^7.15.0",
    "@types/jest": "^27.0.2",
    "babel-jest": "^27.3.0",
    "esbuild": "^0.13.7",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^27.2.5",
    "jsdom": "^18.0.0",
    "typescript": "^4.4.4"
  }
}
